@page
@model WebAppStudies.Pages.MultiplePickModel
@{
    ViewData["Title"] = "Study MultiplePick";
}
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@attribute [IgnoreAntiforgeryToken]
<!-- Page list-of-quotes -->
<div class="container px-4 py-4" id="list-of-quotes">
    <h2 class="pb-2 border-bottom">List of famous quotes</h2>

    <p>Below are some famous quotes about love, work and the fact that tomorrow is another day. Select one that your friend likes.</p>
    <form class="needs-validation" method="post" novalidate>
        <div class="row mb-2 text-center">
            <div class="col-md-10 themed-grid-col">
                <label for="quoteId" class="form-label">Multiple Select</label>
                <select class="form-select" asp-for="SelectedQuoteIds" asp-items="Model.QuoteSelection1"></select>
            </div>
            <div class="col-md-2 themed-grid-col">
                <button class="btn btn-success btn-sm" type="submit">Submit</button>
            </div>
        </div>
    </form>
    <div class="row mb-2 text-center">
        <div class="col-md-10 themed-grid-head-col">
            Selected items
        </div>
    </div>
    @foreach (var quote in Model.SelectedQuotes)
    {
        <div class="row mb-2 text-center">
            <div class="col-md-10 themed-grid-col">
                @quote.Quote
                <div class="col-md-16  text-end">@quote.Author</div>
            </div>
        </div>
    }

    <!-- inital template that can be easily hidden -->
    <div class="d-none">
        <h2 class="pb-2 border-bottom">TEMPLATE</h2>
        <form class="needs-validation" method="post" novalidate>
            <div class="row mb-2 text-center">
                <div class="col-md-10 themed-grid-col">
                    <label for="quoteId" class="form-label">Multiple Select</label>
                    <select class="form-select" name="quoteId" id="quoteId" multiple></select>
                </div>
                <div class="col-md-2 themed-grid-col">
                    <button class="btn btn-success btn-sm" type="submit">Submit</button>
                </div>
            </div>
        </form>
        <div class="row mb-2 text-center">
            <div class="col-md-10 themed-grid-col">
                Submitted Quote
            </div>
        </div>
    </div>
</div>



